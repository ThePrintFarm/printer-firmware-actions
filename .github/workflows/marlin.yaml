name: Build Marlin Firmwares

on:
  workflow_call:
    inputs:
      marlin_branch:
        description: Marlin branch to use
        type: string
        required: false
        default: bugfix-2.1.x
      build_env:
        description: The build 'environment' to use
        required: false
        type: string
        default: "NONE"
      build_obj:
        description: The build object, or resulting firmware
        type: string
        required: true
      config_dir:
        description: The directory holding your configs
        required: true
        type: string
      platformio_config:
        description: The path and filename of the file to use as platformio.ini
        required: false
        type: string
        default: ""
      prefix:
        description: Artifact name to use
        required: true
        type: string
        default: ""

jobs:
  build-marlin:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: pip
      - name: Install PlatformIO
        run: pip install platformio
      - name: Checkout Marlin
        run: git clone https://github.com/MarlinFirmware/Marlin ${{ inputs.build_env }}-${{ steps.date.outputs.tag }}
      - name: Copy configuration into place
        run: |
          cp -av ${{ inputs.config_dir }}/*.h ${{ inputs.build_env }}-${{ steps.date.outputs.tag }}/Marlin/
      - name: Clone and build Marlin ${{ inputs.config_dir }}
        run: |
          cd ${{ inputs.build_env }}-${{ steps.date.outputs.tag }}
          pio run ${{ inputs.build_env != 'NONE' && format('-e {0}', inputs.build_env) || '' }}
          mkdir -vp ${PWD}/../firmware/
          mv -v .pio/build/${{ inputs.build_env }}/${{ inputs.build_obj != 'NONE' && inputs.build_obj || 'firmware.bin' }} ../${{ inputs.prefix }}-${{ inputs.build_env }}-${{ steps.date.outputs.tag }}-${{ inputs.build_obj != 'NONE' && inputs.build_obj || 'firmware.bin' }}
          cd ..
          rm -rf ${{ inputs.build_env }}-${{ steps.date.outputs.tag }}
      - name: Rename build object
        run: |
          mkdir -vp ${{ github.workspace }}/firmware
      # - uses: marvinpinto/action-automatic-releases@latest
      #   with:
      #     repo_token: "${{ secrets.GITHUB_TOKEN }}"
      #     automatic_release_tag: ${{ inputs.prefix }}-${{ steps.date.outputs.tag }}
      #     title: ${{ inputs.prefix }}-${{ steps.date.outputs.tag }}
      #     files: |
      #       ${{ inputs.prefix }}-${{ inputs.build_env }}-${{ steps.date.outputs.tag }}-${{ inputs.build_obj != 'NONE' && inputs.build_obj || 'firmware.bin' }}
